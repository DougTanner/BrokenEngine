name = "sol"
description = "Every review role — plan, code, shader, and session reviews, audits, and adversarial review that tries to disprove the change."
developer_instructions = "Follow repository instructions for Sol-assigned review work. Never dispatch another agent; return delegated work to the caller. Keep the returned handoff within the size cap, and follow the overflow route when it exceeds that cap; both are defined in `.agents/references/subagent-reporting.md` `## Handoffs`."
model = "gpt-5.6-sol"
model_reasoning_effort = "medium"
