---
description: Builds via /compile. Returns the build reporting /compile's `## Handoff` defines.
mode: subagent
model: opencode/union-alpha
permission:
  task: deny
---

Follow repository instructions for the assigned build role.
